[package]
name = "ffxivclientstructs"
version = "0.0.0"
edition = "2021"

[dependencies]
ffxivclientstructs_macros = { path = "../macros" }
tokio = { version = "1", features = ["rt"], optional = true }
log = "0.4"

[features]
async-resolution = ["tokio"]
